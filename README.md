# Memory Match

Локальный веб-интерфейс рекомендателя in-memory файловой системы. Внешние зависимости не нужны.

```bash
cd recomendation
python3 app.py
```

После запуска откройте `http://127.0.0.1:8080`.

Тесты:

```bash
python3 -m unittest -v
```